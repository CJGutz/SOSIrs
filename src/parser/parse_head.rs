use crate::file_rep::head::SOSIHead;

/// Parses the head of a SOSI file.
/// Only send in the HEAD of the SOSI file
pub fn parse_head(head_lines: Vec<&str>) -> Result<SOSIHead, ()> {
    
    todo!()
}
